{
  "type": "function",
  "name": "search_docs",
  "description": "Search the application's internal documentation corpus, not CMS database content. Returns short grounded excerpts for the assistant; returned paths are internal metadata and are not user-accessible. Use for how-to and field meaning questions. Do not use for live entity status — use available live-read tools instead.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Natural language question or keywords"
      },
      "top_k": {
        "type": "integer",
        "minimum": 1,
        "maximum": 10,
        "description": "Number of chunks. Default from host docs_top_k."
      }
    },
    "required": ["query"],
    "additionalProperties": false
  },
  "strict": true
}
