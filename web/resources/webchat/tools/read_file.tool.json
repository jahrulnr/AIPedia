{
  "type": "function",
  "name": "read_file",
  "description": "Read an internal Markdown documentation file for grounding. The returned path is internal metadata and is not user-accessible. Only relative paths inside the docs root are allowed.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Relative Markdown file path inside the docs root."
      },
      "max_chars": {
        "type": "integer",
        "minimum": 1,
        "maximum": 20000
      }
    },
    "required": ["path"],
    "additionalProperties": false
  },
  "strict": true
}
